# =============================================================================
# Local Development Environment Variables
# =============================================================================
# For local Docker Compose setup (not minikube/k8s)
#
# HOW TO APPLY:
#   set -a && source .env.local && set +a
#
# DVC SETUP:
#   1. Switch to local remote:  dvc remote default local
#   2. The 'local' remote in .dvc/config is pre-configured for docker compose MinIO
#
# =============================================================================

# MinIO / S3 (Docker Compose)
S3_ENDPOINT_URL=http://localhost:9000
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=admin123
DVC_REMOTE_URL=s3://dvcstore

# PostgreSQL - PGVector
PGVECTOR_HOST=localhost
PGVECTOR_PORT=5433
PGVECTOR_DATABASE=vectors
PGVECTOR_USER=admin
PGVECTOR_PASSWORD=admin
PGVECTOR_CONNECTION_STRING=postgresql://admin:admin@localhost:5433/vectors

# PostgreSQL - MLflow
MLFLOW_DB_HOST=localhost
MLFLOW_DB_PORT=5432
MLFLOW_DB_NAME=mlflow
MLFLOW_DB_USER=admin
MLFLOW_DB_PASSWORD=admin

# MLflow
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_S3_ENDPOINT_URL=http://localhost:9000