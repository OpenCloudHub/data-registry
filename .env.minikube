# =============================================================================
# Minikube Environment Variables
# =============================================================================
# For usage with minikube (local Kubernetes development)
#
# HOW TO APPLY:
#   set -a && source .env.minikube && set +a
#
# WHAT GOES HERE vs params.py:
#   - Here: Platform infrastructure (URLs, credentials, runtime overrides)
#   - params.py: Pipeline logic defaults (model config, chunk sizes)
#
# In production, these come from Kubernetes ConfigMaps/Secrets.
# =============================================================================

# -----------------------------------------------------------------------------
# S3 / MinIO Configuration (DVC remote storage)
# -----------------------------------------------------------------------------
AWS_ACCESS_KEY_ID=admin
AWS_SECRET_ACCESS_KEY=12345678
AWS_ENDPOINT_URL=https://minio-api.internal.opencloudhub.org
# NOTE: Do NOT set AWS_CA_BUNDLE="" - empty string breaks boto/aiohttp
# If you need to disable SSL verification, use ssl_verify=false in .dvc/config

# -----------------------------------------------------------------------------
# DVC Configuration
# -----------------------------------------------------------------------------
DVC_REPO_URL=https://github.com/OpenCloudHub/data-registry
DVC_REMOTE=minikube
# DVC_DATA_VERSION can be set here to override params.py default
DVC_DATA_VERSION=opencloudhub-readmes-download-v1.0.0

# -----------------------------------------------------------------------------
# PostgreSQL / PGVector (Vector Database)
# -----------------------------------------------------------------------------
# Use port-forwarding to access minikube service:
#   kubectl port-forward -n storage svc/demo-app-db-cluster-rw 5432:5432
PGVECTOR_HOST=127.0.0.1
PGVECTOR_PORT=5432
PGVECTOR_DATABASE=demo_app
PGVECTOR_USER=demo-app
PGVECTOR_PASSWORD=1234
PGVECTOR_TABLE_NAME=readme_embeddings

# -----------------------------------------------------------------------------
# MLflow Tracking
# -----------------------------------------------------------------------------
MLFLOW_TRACKING_URI=https://mlflow.internal.opencloudhub.org
MLFLOW_TRACKING_INSECURE_TLS=true
# -----------------------------------------------------------------------------
# Pipeline Lineage Tracking
# -----------------------------------------------------------------------------
# These are injected by Argo Workflows in production
DOCKER_IMAGE_TAG=DEV
ARGO_WORKFLOW_UID=DEV
